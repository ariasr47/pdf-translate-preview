# Upper bounds are deliberate: PyMuPDF moves surfaces between minor
# releases (TextPage/displaylist shapes, Story engine, insert_htmlbox
# signature), and this pipeline reads several of them directly. Raise a
# bound only with the full suite green on the new version.
#
# pikepdf 9 and 10 differ in behaviour this code depends on: asking a
# non-dictionary object for a key raises on 9 and returns None on 10.
# These are declared compatibility ranges, not a claim every release
# combination has been exercised. constraints-reference.txt records the
# direct versions tested locally; CI records what each job actually resolves.
pymupdf>=1.27.1,<1.30
pikepdf>=10.13.0.post1,<11
fonttools>=4.40,<5
